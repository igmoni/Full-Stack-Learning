# Typescript Pros

- Addon
- Ts doesn't run, it needs a process, it returns js, js runs in the web browser
- type checker
- cause of type checker we get consistency in code

### Type inferencing
- Is the typescript automatically understand whoch data type of varibale it is

### Annotation
- We have to give a data type in lowercase to tell what  kind of values will be stored inside it